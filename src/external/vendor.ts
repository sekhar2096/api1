// tslint:disable-next-line:no-require-imports
import express = require('express');

// tslint:disable-next-line:no-require-imports
import bodyParser = require('body-parser');

// tslint:disable-next-line:no-require-imports
import bcrypt = require('bcrypt');

export { express, bodyParser, bcrypt };
